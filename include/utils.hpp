#ifndef UTILS_HPP
# define UTILS_HPP

# include <vector>
# include <string>
namespace webserver {

	std::vector<std::string>	split(std::string str, const std::string& delim);

} // namespace webserver

#endif